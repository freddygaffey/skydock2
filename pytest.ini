[pytest]
; Flight-stack suite only. tools/log_server/tests has its own conftest and is
; run separately: python -m pytest tools/log_server/tests/
testpaths = tests
norecursedirs = hailo-rpi5-examples archive tools missions real_missions rpi_missions sim_data .venv
addopts = -q
